var strike=strike.toFixed(2); //for calculating the strike rate ends with two decimal degits


====verification and validation ===
- the password should be of minimum 8 characters having atleast 1 small letter 1 capital letter 1 digit and 1 special character
/^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*\W)[^ ]{8,}$/;


